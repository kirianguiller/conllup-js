import {
  sentenceConllToJson,
  sentenceJsonToConll,
  emptySentenceJson,
  emptyMetaJson,
  emptyTreeJson,
  emptyTokenJson,
  emptyFeatureJson,
  replaceArrayOfTokens,
} from './conll';

export default {
  sentenceConllToJson,
  sentenceJsonToConll,
  emptySentenceJson,
  emptyMetaJson,
  emptyTreeJson,
  emptyTokenJson,
  emptyFeatureJson,
  replaceArrayOfTokens,
};
